env:
  global:
    Label: Taisia is beautiful

linux:
  os: linux
  dist: xenial
  sudo: false
  language: python
  python: 3.7

install:
  - python --version
  - pip install conan
  - pip install nose2

script:
  - nose2 -v
